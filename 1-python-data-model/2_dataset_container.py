class ImageDataset:
    """Create a dataset that behaves like a Python sequence"""
    
    def __init__(self, image_paths, labels):
        # TODO: Implement using __len__, __getitem__
        pass
    
    # Make it work with: len(dataset), dataset[0], for item in dataset